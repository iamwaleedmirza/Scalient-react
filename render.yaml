services:
  - type: web
    name: scalient-react
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        generateValue: false
      - key: JWT_SECRET
        generateValue: false
